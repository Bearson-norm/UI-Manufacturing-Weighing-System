version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: manufacturing_db
    environment:
      POSTGRES_DB: kmi_manufacturing_db
      POSTGRES_USER: manufacturing_user
      POSTGRES_PASSWORD: manufacturing_password
      TZ: Asia/Bangkok
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./server/database/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    networks:
      - manufacturing_network

  # Backend API
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    container_name: manufacturing_api
    environment:
      - NODE_ENV=production
      - TZ=Asia/Bangkok
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=kmi_manufacturing_db
      - DB_USER=manufacturing_user
      - DB_PASSWORD=manufacturing_password
      - PORT=6657
      - JWT_SECRET=your_super_secret_jwt_key_here
      - CORS_ORIGIN=https://kmi-mows.moof-set.web.id
    ports:
      - "6657:6657"
    depends_on:
      - postgres
    networks:
      - manufacturing_network
    restart: unless-stopped

  # Frontend React App
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: manufacturing_frontend
    environment:
      - REACT_APP_API_URL=http://localhost:6657/api
    ports:
      - "3000:80"
    depends_on:
      - api
    networks:
      - manufacturing_network
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  manufacturing_network:
    driver: bridge


